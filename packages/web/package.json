{
    "name": "@jk/web",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "docker build -t $IMAGE_NAME .",
        "push": "docker push $IMAGE_NAME",
        "deploy": "npm run build && npm run push"
    },
    "devDependencies": {
        "dotenv-cli": "^7.0.0"
    }
}
