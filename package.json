{
  "name": "single-page-website",
  "version": "1.0.0",
  "description": "A minimal Pulumi program to deploy a single-page website on k3s",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "pulumi up"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/kubernetes": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "typescript": "^4.0.0"
  }
}
