services:
  whoami:
    build:
      context: ./apps/whoami
      dockerfile: Dockerfile
    container_name: whoami
    restart: always
    environment:
      MONGO_WEB_PASSWORD: ${MONGO_WEB_PASSWORD:-webpassword}
      VIRTUAL_HOST: "whoami.${DOMAIN:-localhost}"
      WITH_AUTH_HEADERS: true
    depends_on:
      - mongodb
    expose:
      - "3000"
      
  shorty-webserver:
    image: nginx:latest
    container_name: shorty-webserver
    restart: always
    volumes:
      - www:/var/www
      - ./nginx/shorty.conf:/etc/nginx/conf.d/shorty.conf
    environment:
      VIRTUAL_HOST: "shorty.${DOMAIN:-localhost}"
    expose:
      - "80"